import 'package:flutter/material.dart';

class SurapatiCard extends StatelessWidget {
  const SurapatiCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
